# No "v" prefix on tags created by `npm version` — matches every tag
# already in this repo (1.1.0, 1.2.0, ...) and the release workflow's
# trigger pattern (.github/workflows/release.yml), which only matches
# plain X.Y.Z tags.
tag-version-prefix=
